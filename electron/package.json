{
  "name": "xrh-dfui",
  "version": "1.0.0",
  "description": "向瑞豪的Delta Force UI with Ave Mujica",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "node copy-dist.js && node clean-dist.js && electron-builder --config.asar=false --dir --x64 --config.directories.output=app-dist"
  },
  "keywords": [],
  "author": "向瑞豪",
  "packageManager": "pnpm@10.26.2",
  "devDependencies": {
    "electron": "^39.2.7",
    "electron-builder": "^26.0.12",
    "electron-is-dev": "^3.0.1",
    "fs-extra": "^11.3.3"
  }
}
